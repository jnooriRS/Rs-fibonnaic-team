jira smart commit working?
 code to run coverge
 pytest tests/ -vv --cov=. --cov-branch --cov-report=term-missing --cov-report html:reports/coverage/cov_html --cov-report=xml:reports/coverage/coverage.xml --junitxml=reports/xunit/test-results.xml -o junit_family=xunit1

1. .pre-commit config file
2. add black =
3. run pre-commit install
5. When adding new hooks to .pre-commit
6. Issues with semgrep installed via pyhton, however, docker image is working
7. Why does web-broweser not change to result when form submit it pressed? Still says home
